.wrapper
  .chat-menu
    = render 'shared/side_bar'

  .chat-main
    = render partial: "main-header", locals: { group: @group }
    .messages
      = render partial: 'message', collection: @messages
    .form
      = render partial: "new-message"
